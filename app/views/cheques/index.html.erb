<p id="notice"><%= notice %></p>

<h1>Listing Cheques</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Creation</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cheques.each do |cheque| %>
      <tr>
        <td><%= cheque.name %></td>
        <td><%= cheque.creation %></td>
        <td><%= cheque.value %></td>
        <td><%= link_to 'Show', cheque %></td>
        <td><%= link_to 'Edit', edit_cheque_path(cheque) %></td>
        <td><%= link_to 'Destroy', cheque, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cheque', new_cheque_path %>
